<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/Task_managment_system_db?createDatabaseIfNotExist=true</property>
        <property name="connection.username">your_db_username</property> <!-- TODO: Update with your actual database username -->
        <property name="connection.password">your_db_password</property> <!-- TODO: Update with your actual database password -->

        <!-- Hibernate dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Format the SQL in stdout -->
        <property name="format_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Names the annotated entity classes -->
        <mapping class="com.example.tasksystem.server.entities.User"/>
        <mapping class="com.example.tasksystem.server.entities.Project"/>
        <mapping class="com.example.tasksystem.server.entities.Task"/>

    </session-factory>
</hibernate-configuration>
