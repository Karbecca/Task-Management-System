<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/Task_managment_system_db?createDatabaseIfNotExist=true&amp;useSSL=false&amp;serverTimezone=UTC</property>
        <property name="hibernate.connection.username">your_db_username</property> <!-- TODO: Replace with your DB username -->
        <property name="hibernate.connection.password">your_db_password</property> <!-- TODO: Replace with your DB password -->

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="hibernate.show_sql">true</property>

        <!-- Format the SQL in stdout -->
        <property name="hibernate.format_sql">true</property>

        <!-- Automatically validates or exports schema DDL to the database when the SessionFactory is created -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mention annotated entity classes -->
        <mapping class="com.example.tasksystem.server.model.Admin"/>
        <mapping class="com.example.tasksystem.server.model.AppUser"/>
        <mapping class="com.example.tasksystem.server.model.Task"/>
        <mapping class="com.example.tasksystem.server.model.TaskAssignment"/>
        <!-- RoleEnum is not listed here as it's handled by @Enumerated in Admin entity -->

    </session-factory>
</hibernate-configuration>
